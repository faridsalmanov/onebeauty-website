/** Hero seam uses `var(--ob-site-atmosphere-bg)` + fixed attachment (see globals.css + SiteAtmosphere). */

/** Pinned top wash — strong toward top (meets hero) so the pair reads as one blend */
export const PINNED_SEAM_BLUE_GRADIENT =
  "linear-gradient(180deg, rgb(10 21 40 / 0.88) 0%, rgb(19 36 62 / 0.48) 38%, rgb(10 21 40 / 0.16) 68%, transparent 100%)";
